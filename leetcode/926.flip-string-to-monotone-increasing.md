# [926. Flip String to Monotone Increasing](https://leetcode.com/problems/flip-string-to-monotone-increasing/)

## Enumeration
A monotone increasing string like `000111`, `0000`, or `1111` always has a "pivot", so that everything before it is `0` and everything after it is `1`. The string can be split into two halves (the pivot itself is the **left** part):
```js
 0, 0, ...   i | 1, 1, 1, ...
[only 0's to i] [only 1's after i]
```

> Or we can define the pivot to be the "right" part, so that everything before (0 ~ `i - 1`) it is `0` and everything after it (including `i` itself) is `1`:
>
> ```js
> 0, 0, ...  | i, 1, 1, 1, ...
> |--------| |----------------|
> ```

For every pivot, we have to flip everything to `0` before it, and everything to `1` after it, so the number of flips is `(Number of 1s on the left)` + `(Number of 0s on the right)`

```js
0, 1, 0, 1, 0, 0, 1, 1, 0
         i|
   0     0  1  1        1   // Flip
          |
[# of 1's]|[# of 0's]       // The number of flips
          |

// Example:
    0, 1, 1, 0, 0, 0   flips
 i  1        1  1  1 = 0 + 4
    i        1  1  1 = 0 + 3
       i     1  1  1 = 0 + 3
       0  i  1  1  1 = 1 + 3
       0  0  i  1  1 = 2 + 2
       0  0     i  1 = 2 + 1
       0  0        i = 2 + 0 (min)
```

There are some details to pay attentions:
* We include the current index `i` itself to be **left** part, so we have to flip if it's `1` and don't flip if it's `0`.
```js
0, 1, 0, 1, 0, 0, 1, ...
         i|
   F     F

1, 1, 0, 0, 1, 0, ...
         i|
F  F
```
* We must initialize `minFlips` to be **the number of 0's after `i`**. It represents that left side is entirely empty, and the right side is the entire string, then we have to flip all 0's to 1's. (This is the case we flip the entire string to 1's)

```js
    0, 1, 1, 0, 0, 0   flips
i|  F        F  F  F       4
```

* Q: How do we handle the flip to "000..." or "111..."? A: Before the iteration, we initialize `minFlips` to be the number of 0's after `i`, this is for the case to flip to "111...". When `i == n - 1`, we flip the entire string to 0's, this is the case we flip to "000...".

* How to calculate `(Number of 1s on the left)` + `(Number of 0s on the right)`? We use a prefix sum of 0's and 1's for left side. For right side deduction, it's simply `Total 0s - 0s seen so far`:

```kotlin
fun minFlipsMonoIncr(s: String): Int {
    val n = s.length
    val total0 = s.count { it == '0' }
    val total1 = s.count { it == '1' }
    var left0 = 0
    var left1 = 0
    var minFlips = total0
    for (i in s.indices) {
        if (s[i] == '0') left0++ else left1++
        
        val right0 = total0 - left0
        minFlips = minOf(minFlips, left1 + right0)
    }
    return minFlips
}

// Or the current index is the "right" part.
// Pay attention to the initialization and the order of calculation.
fun minFlipsMonoIncr(s: String): Int {
    var total0 = s.count { it == '0' }
    var total1 = s.count { it == '1' }
    var before0 = 0
    var before1 = 0
    var minFlips = total1
    for (i in s.indices) {
        val after0 = total0 - before0
        minFlips = minOf(minFlips, before1 + after0)
        if (s[i] == '0') before0++
        else before1++
    }
    return minFlips
}
```

> 我自己在這題卡了很久、一直卡在一些點：
> - 該怎麼翻轉才會讓最終翻轉次數最少？
> - 切分點「自己」該怎麼處理？
> - 假設我之前做一些翻轉，那到現在這位置該怎麼「記住」之前做過的翻轉？
> - 每個切分，都會去翻轉之前的所有 1’s、以及後面所有的 0’s，最後變成前面是 0、後面是 1 的情況，但是為何不用看全部變成 0’s 或者全部變成 1’s 的情況？

## Enumeration 2
We itearte the array from left to right, and there are some possible cases:
1. Current is `0`, and haven't seen any `1` before it: It's fine, cost = 0.
2. Current is `1`, and haven't seen any `1` yet: It's fine, we keep track of how many `1's` we've seen, they are potential "liabilities" if we see a `0` later.
3. Current is `0` and we've seen `1` before (The Conflict): It breaks the monotone property. We have two choices:
    - Flip the current `0` to `1`: Cost increments by 1.
    - Flip all previous `1's` to `0's`: Cost is the number of `1's` seen so far.
    - We take the minimum of the two costs.
4. Current is `1` and we've seen `1` before: See below.

```kotlin
fun minFlipsMonoIncr(s: String): Int {
    var count1 = 0
    var flipsCount = 0
    for (c in s) {
        if (c == '0') {
            val flipCurrent0 = flipsCount + 1
            val flipAllPrevious1 = count1
            flipsCount = minOf(flipCurrent0, flipAllPrevious1)
        } else {
            count1++
        }
    }
    return flipsCount
}
```

> Q: 為什麼遇到 `1` 前面也遇過 `1` 時 (Case 4)，我們「不需要」考慮把它翻轉成 `0`？

你可以從**「單調遞增 (Monotone Increasing) 的定義」以及「數學上的不變性 (Invariant)」**來思考：

1. **單調遞增的包容性：**

   一個單調遞增的字串，結尾可以是 `0`，也可以是 `1`。

   如果我們過去的字串「已經」滿足單調遞增（我們花了 `flips` 次把它整理好），現在新來了一個字元 `1`。因為任何單調遞增的字串後面接上 `1`，**它依然是單調遞增的**（例如：`000 -> 0001`，或是 `011 -> 0111`）。所以，保留它是 `1` 的成本是 **0**，我們當下的 `flips` 次數完全不用增加。

2. **如果硬要翻轉成 `0` 會發生什麼事？**

   如果你選擇把當下的 `1` 翻轉成 `0`，為了解持單調遞增，這意味著它前面的**所有字元都必須是 `0`**。

   那成本會是多少？成本就是把前面所有的 `1` 都翻掉，加上翻轉當下這個 `1` 的成本，也就是 `count1`。

   但在你的 Case 3 中，我們每次遇到 `0` 都會做 `flips = min(ones, flips + 1)`，這個操作保證了一個不變性（Invariant）：**我們紀錄的 `flips` 永遠小於或等於 `count1`**。

   既然保持為 `1` 的成本（`flips`）永遠 <= 翻轉成 `0` 的成本（`ones`），那麼我們遇到 `1` 時，自然無腦保持原樣、只記錄 `1` 的數量就好了！